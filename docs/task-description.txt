Backend & Frontend DEVs Task One
---------------------------------

Using the attached PRD md file, you should implement this project backend, frontend codes, software testing verifications using playwright MCP server, and unit testing as optional additional verification

Hints:
-----

Hint 1: you need implement this project using "Claude Loops"

Hint 2: these Claude Loops must be reusable can run and implement any application by take requirements as full project requirements or single US and implement them, so Claude loops must can use all of them through orchestrator or direct use any of them 

Hint 3: you can use these helper websites to search for useful skills/MCPs to use them or you can build yours also you can use spec-kit skills and workflow

https://www.skills.sh/
https://github.com/mattpocock/skills
https://mcpservers.org/
https://mcp.directory/
https://github.com/github/spec-kit

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Required project loops:
-------------------------

loop-1: backend-dev (input: PRD md file, output: backend phases md files in outputs/ of this loop with the tasks of each phase and with mark the achieved task in them and in the progress.md and state/ files then create the code using your selected stack, and swager for backend endpoints then test each implemented phase after finish it using curl commands, process: implement the backend code development planning and implementation and test it using curl commands and generate the swagre, optional: add unit testing)

loop-2: frontend-dev (input: PRD md file and backend swagger, output: frontend phases md files in outputs/ of this loop with the tasks of each phase and with mark the achieved task in them and in the progress.md and state/ files then implement frontend code using your selected stack and UI artifact URL, then this URL using playwright MCP for each feature/phase implementation, process: plan and implement the frontend code development and test the URL as each feature done using playwright MCP, optional: add unit testing)

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Loop structure:
------------------

Loop-instructions.md
task.md
progress.md , should represent all action items implemented and log the start and end time and token consumption for each milestone implementation
state/
stop condition: work milestone complete correctly or 3 trials
outputs/: contains md file for each spec or milestone implementation

You can select your design to implement all backend codes or all frontend codes firstly then start the other or generate a plan and tasks with priority and can implement parallel and dependency graph 

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

##Deliverables for the full task:
------------------------------------

GitHub repo contains:
--------------------

Loops structures and outputs of each loop
codes and run artifacts 
excel file: contains all used prompts and sessions ids the used to implement the project for all steps
document file written from you not AI generated for how you design and implement the work
readme file for the loops with sequence diagrams for the loops using mermaid

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Hope you enjoy, thank you in advance for your efforts